# Second Brain Python dependencies
# Install with: pip install -r requirements.txt
# Python requirement: 3.10+

# Flask web prototype
flask
mcp

# Jupyter notebooks
jupyter
notebook
ipykernel

# Embeddings and vector search
sentence-transformers
numpy

# SQL safety gate for LLM-generated read-only SQL
sqlglot

# Qwen LLM fallback for the retired prototype.
# llama-cpp-python matches the Android target: llama.cpp + GGUF.
llama-cpp-python

# Alternative backend, if llama-cpp-python is not practical on a machine:
# transformers
# accelerate
# torch

# Historical local adapter inference used a separate GPU environment and
# infer_finetuned_parser.py. The active Android app does not require it.
